#ifndef HEADER_FILE
#define HEADER_FILE
#include <stddef.h>
#include <stddef.h>
#include <stdarg.h>

int _printf(const char *format, ...);
int _puts(char *str);
int _putchar(char c);
int print_int(long int num);
#endif
